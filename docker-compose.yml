services:
  scrap-conventions:
    build: .
    container_name: scrap-conventions
    restart: unless-stopped
    ports:
      - "5050:5000"
    volumes:
      - /share/scrap-conventions/cache:/app/cache
